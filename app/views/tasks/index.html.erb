<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>
<% @tasks.each do |t| %>
  <h2><%= link_to t.name, task_path(t) %></h2>
    <p><%= t.details %></p>
 <%= link_to "modifie", edit_task_path(t) %>
<% end %>
  <p><%= link_to "Add new task", new_task_path%></p>
